# sets the nginx internal variable $lagoobernetes_environment_type to the env variable LAGOOBERNETES_ENVIRONMENT_TYPE
set_by_lua_block $lagoobernetes_environment_type { return os.getenv("LAGOOBERNETES_ENVIRONMENT_TYPE") }